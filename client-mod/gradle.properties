org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
org.gradle.configuration-cache=false

minecraft_version=1.21.11
# Compile against the oldest Loader accepted at runtime. The Mod only uses
# Fabric API entrypoints and does not depend on newer Loader implementation APIs.
loader_version=0.17.3
loom_version=1.17.18
fabric_api_version=0.141.6+1.21.11

mod_version=3.1.3
maven_group=cn.yjj.homingmissiles
